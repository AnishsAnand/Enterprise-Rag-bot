# Multi-Agent System Architecture

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                           │
│                    (Frontend / API Client)                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP POST /api/agent/chat
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FastAPI Application                         │
│                    (app/api/routes/agent_chat.py)                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                       Agent Manager                              │
│                  (app/agents/agent_manager.py)                   │
│  • Initializes all agents                                        │
│  • Manages agent lifecycle                                       │
│  • Provides unified interface                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    OrchestratorAgent                             │
│              (app/agents/orchestrator_agent.py)                  │
│  • Main coordinator and router                                   │
│  • Decides which agent handles each step                         │
│  • Manages conversation flow                                     │
│  • Synthesizes responses                                         │
└──────┬──────────┬──────────┬──────────┬──────────────────────────┘
       │          │          │          │
       │          │          │          │
       ▼          ▼          ▼          ▼
┌──────────┐┌──────────┐┌──────────┐┌──────────┐
│  Intent  ││Validation││Execution ││   RAG    │
│  Agent   ││  Agent   ││  Agent   ││  Agent   │
└────┬─────┘└────┬─────┘└────┬─────┘└────┬─────┘
     │           │           │           │
     └───────────┴───────────┴───────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Supporting Services                           │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ Conversation    │  │ API Executor    │  │ Vector DB       │ │
│  │ State Manager   │  │ Service         │  │ (Milvus)        │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    External Services                             │
│  • K8s Cluster API                                               │
│  • Firewall API                                                  │
│  • Load Balancer API                                             │
│  • Database API                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🤖 Agent Responsibilities

### OrchestratorAgent (Base Coordinator)
```
┌─────────────────────────────────────────┐
│         OrchestratorAgent               │
├─────────────────────────────────────────┤
│ Responsibilities:                       │
│ • Route requests to specialized agents  │
│ • Manage conversation flow              │
│ • Coordinate between agents             │
│ • Synthesize responses                  │
│ • Handle agent handoffs                 │
│                                         │
│ Decision Logic:                         │
│ • Action keywords → IntentAgent         │
│ • Question keywords → RAGAgent          │
│ • Missing params → ValidationAgent      │
│ • Ready to execute → ExecutionAgent     │
└─────────────────────────────────────────┘
```

### IntentAgent
```
┌─────────────────────────────────────────┐
│            IntentAgent                  │
├─────────────────────────────────────────┤
│ Responsibilities:                       │
│ • Detect user intent                    │
│ • Identify resource type                │
│ • Identify operation (CRUD)             │
│ • Extract parameters from text          │
│ • Determine required parameters         │
│                                         │
│ Input:  "Create a cluster named prod"   │
│ Output: {                               │
│   resource_type: "k8s_cluster",         │
│   operation: "create",                  │
│   extracted_params: {name: "prod"},     │
│   required_params: [...]                │
│ }                                       │
└─────────────────────────────────────────┘
```

### ValidationAgent
```
┌─────────────────────────────────────────┐
│          ValidationAgent                │
├─────────────────────────────────────────┤
│ Responsibilities:                       │
│ • Validate parameters against schema    │
│ • Collect missing parameters            │
│ • Ask clarifying questions              │
│ • Extract params from user responses    │
│ • Ensure data quality                   │
│                                         │
│ Validation Rules:                       │
│ • Type checking (string, int, bool)     │
│ • Length validation (min/max)           │
│ • Range validation (numeric)            │
│ • Pattern matching (regex)              │
│ • Enum validation                       │
└─────────────────────────────────────────┘
```

### ExecutionAgent
```
┌─────────────────────────────────────────┐
│          ExecutionAgent                 │
├─────────────────────────────────────────┤
│ Responsibilities:                       │
│ • Execute validated operations          │
│ • Make API calls                        │
│ • Handle success/error responses        │
│ • Provide user-friendly feedback        │
│ • Format results                        │
│                                         │
│ Execution Flow:                         │
│ 1. Check permissions                    │
│ 2. Final validation                     │
│ 3. Make API call                        │
│ 4. Handle response                      │
│ 5. Format feedback                      │
└─────────────────────────────────────────┘
```

### RAGAgent
```
┌─────────────────────────────────────────┐
│             RAGAgent                    │
├─────────────────────────────────────────┤
│ Responsibilities:                       │
│ • Answer documentation questions        │
│ • Search vector database                │
│ • Retrieve relevant context             │
│ • Generate context-aware responses      │
│ • Cite sources                          │
│                                         │
│ RAG Flow:                               │
│ 1. Receive question                     │
│ 2. Search vector DB (Milvus)            │
│ 3. Retrieve top-k documents             │
│ 4. Generate response with context       │
│ 5. Add source citations                 │
└─────────────────────────────────────────┘
```

## 🔄 Conversation Flow

### Example: Creating a K8s Cluster

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Initial Request                                         │
└─────────────────────────────────────────────────────────────────┘
User: "Create a new Kubernetes cluster"
         │
         ▼
OrchestratorAgent: Detects action intent
         │
         ▼
Routes to: IntentAgent
         │
         ▼
IntentAgent: 
  • Detects: resource=k8s_cluster, operation=create
  • Extracts: (no params yet)
  • Identifies required: [name, region, version, ...]
         │
         ▼
Routes to: ValidationAgent
         │
         ▼
ValidationAgent: "I need: Cluster Name, Region, Version..."

┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Parameter Collection                                    │
└─────────────────────────────────────────────────────────────────┘
User: "Name it prod-01, version 1.28, in us-east-1"
         │
         ▼
ValidationAgent:
  • Extracts: {name: "prod-01", version: "1.28", region: "us-east-1"}
  • Validates: ✓ name valid, ✓ version valid, ✓ region valid
  • Still missing: [control_plane_type, business_unit, ...]
         │
         ▼
ValidationAgent: "Great! I still need: Control Plane Type..."

┌─────────────────────────────────────────────────────────────────┐
│ Step 3: Complete Collection                                     │
└─────────────────────────────────────────────────────────────────┘
User: "Standard control plane, Engineering BU"
         │
         ▼
ValidationAgent:
  • Extracts: {control_plane_type: "Standard", business_unit: "Engineering"}
  • Validates: ✓ All parameters collected and valid
  • Status: READY_TO_EXECUTE
         │
         ▼
ValidationAgent: "Perfect! Shall I proceed?"

┌─────────────────────────────────────────────────────────────────┐
│ Step 4: Execution                                               │
└─────────────────────────────────────────────────────────────────┘
User: "Yes, proceed"
         │
         ▼
OrchestratorAgent: Routes to ExecutionAgent
         │
         ▼
ExecutionAgent:
  • Checks permissions: ✓ User has 'admin' role
  • Final validation: ✓ All params valid
  • Makes API call: POST /v1/clusters
  • Receives response: {cluster_id: "cls-123", status: "provisioning"}
         │
         ▼
ExecutionAgent: "✅ Successfully created cluster!
                 Cluster ID: cls-123
                 Status: Provisioning"
```

## 💾 State Management

### Conversation State Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                    Conversation State                           │
└─────────────────────────────────────────────────────────────────┘

INITIATED
    │
    │ Intent detected
    ▼
COLLECTING_PARAMS
    │
    │ Parameters being collected
    ▼
VALIDATING
    │
    │ All params collected
    ▼
READY_TO_EXECUTE
    │
    │ User confirms
    ▼
EXECUTING
    │
    ├─ Success ──→ COMPLETED
    │
    └─ Failure ──→ FAILED

State Contains:
┌────────────────────────────────────┐
│ • session_id                       │
│ • user_id                          │
│ • intent (resource + operation)    │
│ • required_params: Set[str]        │
│ • collected_params: Dict[str, Any] │
│ • missing_params: Set[str]         │
│ • invalid_params: Dict[str, str]   │
│ • conversation_history: List       │
│ • agent_handoffs: List             │
│ • execution_result: Dict           │
└────────────────────────────────────┘
```

## 🔐 Permission System

```
┌─────────────────────────────────────────────────────────────────┐
│                    Permission Flow                              │
└─────────────────────────────────────────────────────────────────┘

User Request
    │
    ▼
Extract user_roles from request
    │
    ▼
Identify operation (create/delete/update/list)
    │
    ▼
Look up required permissions in resource schema
    │
    ▼
Check: user_roles ∩ required_permissions ≠ ∅
    │
    ├─ Yes ──→ Proceed with operation
    │
    └─ No ───→ Return permission denied error

Example Resource Schema:
┌────────────────────────────────────┐
│ "k8s_cluster": {                   │
│   "permissions": {                 │
│     "create": ["admin", "developer"]│
│     "delete": ["admin"],           │
│     "update": ["admin", "developer"]│
│     "read":   ["admin", "developer",│
│                "viewer"]           │
│   }                                │
│ }                                  │
└────────────────────────────────────┘
```

## 📊 Data Flow

### Request → Response Flow

```
1. HTTP Request
   POST /api/agent/chat
   {
     "message": "Create a cluster",
     "user_id": "user123",
     "user_roles": ["admin"]
   }
        │
        ▼
2. Agent Manager
   • Get/create session
   • Initialize agents if needed
        │
        ▼
3. Orchestrator
   • Analyze message
   • Decide routing
   • Pass to specialized agent
        │
        ▼
4. Specialized Agent
   • Process request
   • Update conversation state
   • Return result
        │
        ▼
5. Orchestrator
   • Synthesize response
   • Add metadata
        │
        ▼
6. HTTP Response
   {
     "success": true,
     "response": "I need more information...",
     "session_id": "abc-123",
     "routing": "intent",
     "metadata": {...}
   }
```

## 🛠️ Technology Stack

```
┌─────────────────────────────────────────────────────────────────┐
│                      Technology Stack                           │
├─────────────────────────────────────────────────────────────────┤
│ Framework:        LangChain (Agent Framework)                   │
│ Web Framework:    FastAPI                                       │
│ LLM:              OpenAI-compatible (Grok)                      │
│ Vector DB:        Milvus                                        │
│ State:            In-memory (ConversationStateManager)          │
│ Validation:       Pydantic + Custom Schema                      │
│ API Client:       httpx (async)                                 │
│ Language:         Python 3.10+                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔌 Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                    Integration Points                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 1. LLM Service (Grok)                                           │
│    • Used by: All agents                                        │
│    • Purpose: Natural language understanding                    │
│    • Config: GROK_BASE_URL, OPENAI_API_KEY                      │
│                                                                 │
│ 2. Vector Database (Milvus)                                     │
│    • Used by: RAGAgent                                          │
│    • Purpose: Documentation retrieval                           │
│    • Config: MILVUS_HOST, MILVUS_PORT                           │
│                                                                 │
│ 3. External APIs                                                │
│    • Used by: ExecutionAgent → APIExecutorService               │
│    • Purpose: CRUD operations on resources                      │
│    • Config: resource_schema.json, API_AUTH_TOKEN               │
│                                                                 │
│ 4. Existing RAG System                                          │
│    • Used by: RAGAgent                                          │
│    • Purpose: Leverage existing knowledge base                  │
│    • Integration: Seamless                                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📈 Scalability

```
Current Architecture:
┌────────────────────────────────────┐
│ Single Instance                    │
│ • In-memory state                  │
│ • Synchronous agent coordination   │
│ • Good for: 100s of concurrent     │
│   conversations                    │
└────────────────────────────────────┘

Future Enhancements:
┌────────────────────────────────────┐
│ Distributed Architecture           │
│ • Message queue for agent comm     │
│ • Horizontal scaling               │
│ • Good for: 1000s of concurrent    │
│   conversations                    │
└────────────────────────────────────┘
```

---

**Architecture designed for**: Maintainability, Extensibility, Scalability

